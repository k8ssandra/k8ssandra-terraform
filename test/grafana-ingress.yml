apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: reaper
  namespace: default
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: test-grafana
          servicePort: 80
        path: /*
        pathType: ImplementationSpecific